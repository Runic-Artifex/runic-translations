# Text Resources package consumer

This executable restores the runtime, generator, and Build packages from an isolated
local feed, verifies the exact runtime/compiler/generator/Build/tool package split,
and exercises generated typed APIs plus the public external-pack path. Pass the feed
path with `--feed`.
