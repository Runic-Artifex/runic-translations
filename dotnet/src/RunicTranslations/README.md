# RunicTranslations

NativeAOT-compatible runtime contracts, compiled snapshots, locale switching,
formatting, fallback, and verified external localization packs for Runic Text
Resources.

This package has no UI-framework dependency. Pair it with
`RunicTranslations.Generator` for typed generated accessors.
