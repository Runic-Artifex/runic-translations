<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>$(RunicTextResourcesGeneratorTargetFramework)</TargetFramework>
    <AssemblyName>RunicTextResources.Generator</AssemblyName>
    <RootNamespace>RunicTextResources.Generator</RootNamespace>
    <Description>Incremental C# source generator for Runic Text Resources.</Description>
    <IsPackable>true</IsPackable>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <DevelopmentDependency>true</DevelopmentDependency>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
    <NuspecFile>RunicTextResources.Generator.nuspec</NuspecFile>
    <CentralPackageVersionOverrideEnabled>true</CentralPackageVersionOverrideEnabled>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
    <!-- The repository deliberately requires a .NET 10 compiler host for every artifact. -->
    <NoWarn>$(NoWarn);RS1041</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" VersionOverride="4.14.0" PrivateAssets="all" />
    <ProjectReference Include="..\RunicTextResources.Compiler\RunicTextResources.Compiler.csproj" PrivateAssets="all" />
  </ItemGroup>

  <Target Name="SetTextResourcesGeneratorNuspecProperties" BeforeTargets="GenerateNuspec">
    <PropertyGroup>
      <NuspecProperties>configuration=$(Configuration);packageVersion=$(PackageVersion)</NuspecProperties>
    </PropertyGroup>
  </Target>
</Project>
