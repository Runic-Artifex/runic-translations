name: Set up Bun workspace
description: Install the pinned Node and Bun toolchains and restore Bun's download cache.

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: 24.18.0
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: 1.4.0
    - uses: actions/cache@v4
      with:
        path: ~/.bun/install/cache
        key: bun-${{ runner.os }}-${{ runner.arch }}-1.4.0-${{ hashFiles('web/bun.lock') }}
        restore-keys: |
          bun-${{ runner.os }}-${{ runner.arch }}-1.4.0-
